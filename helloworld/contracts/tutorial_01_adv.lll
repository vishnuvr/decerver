; Contract that lets you store a two-slot file hash using the file name as key.
; This does not use any helpers, definitions, or shorthand notation.

(return 0x0 
	(lll 
		(if 
			(sload (calldataload 0)) 
			(seq (mstore 0x0 0) (return 0x0 32) )
			(seq
				(sstore (calldataload 0) (calldataload 32))
				(sstore (+ 1 (calldataload 0)) (calldataload 64))
				(mstore 0x0 0)
				(return 0x0 32)
			)
		) 
	0x0 )
)
